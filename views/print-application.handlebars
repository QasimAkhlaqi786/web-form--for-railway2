<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Print Application - {{application.serial_no}}</title>
    <style>
        @page {
            size: A4;
            margin: 20mm;
        }

        @media print {
            body {
                font-family: 'Arial', sans-serif;
                margin: 0;
                padding: 0;
                color: #000;
                font-size: 12px;
                line-height: 1.4;
            }
            
            .print-container {
                width: 100%;
                max-width: 210mm;
                margin: 0 auto;
            }
            
            .print-header {
                text-align: center;
                margin-bottom: 20px;
                border-bottom: 2px solid #000;
                padding-bottom: 15px;
            }
            
            .print-header h1 {
                margin: 0;
                font-size: 18px;
                color: #000;
                text-transform: uppercase;
                letter-spacing: 1px;
            }
            
            .print-header .serial-no {
                font-size: 14px;
                font-weight: bold;
                margin: 8px 0;
                color: #333;
            }
            
            .applicant-container {
                display: flex;
                gap: 20px;
                margin-bottom: 25px;
                align-items: flex-start;
            }
            
            .photo-section {
                flex: 0 0 120px;
                text-align: center;
                border: 1px solid #000;
                padding: 10px;
                background: #fff;
            }
            
            .candidate-photo {
                width: 100px;
                height: 120px;
                object-fit: cover;
                border: 1px solid #ccc;
                margin: 0 auto;
                display: block;
            }
            
            .photo-label {
                font-size: 10px;
                font-weight: bold;
                margin-top: 5px;
                text-transform: uppercase;
            }
            
            .applicant-info {
                flex: 1;
                border: 1px solid #000;
                padding: 15px;
                background: #f9f9f9;
            }
            
            .info-grid {
                display: grid;
                grid-template-columns: repeat(2, 1fr);
                gap: 8px;
            }
            
            .info-item {
                margin: 6px 0;
                page-break-inside: avoid;
            }
            
            .info-item strong {
                display: inline-block;
                width: 100px;
                font-weight: bold;
            }
            
            .documents-section {
                margin: 20px 0;
                border: 1px solid #000;
                padding: 15px;
                background: #f9f9f9;
            }
            
            .documents-section h3 {
                margin: 0 0 10px 0;
                font-size: 13px;
                border-bottom: 1px solid #000;
                padding-bottom: 5px;
            }
            
            .documents-list {
                display: grid;
                grid-template-columns: repeat(2, 1fr);
                gap: 5px;
                font-size: 11px;
            }
            
            .document-item {
                margin: 3px 0;
            }
            
            .signature-area {
                margin-top: 40px;
                padding-top: 10px;
                border-top: 1px solid #000;
                text-align: center;
            }
            
            .signature-line {
                width: 200px;
                border-bottom: 1px solid #000;
                margin: 0 auto;
                padding-top: 30px;
            }
            
            .signature-label {
                font-size: 10px;
                font-weight: bold;
                margin-top: 2px;
            }
            
            .footer {
                margin-top: 30px;
                text-align: center;
                font-size: 10px;
                color: #666;
                border-top: 1px solid #ccc;
                padding-top: 10px;
            }
            
            .stamp-area {
                position: absolute;
                right: 20mm;
                bottom: 30mm;
                text-align: center;
            }
            
            .stamp {
                width: 80px;
                height: 80px;
                border: 2px solid #000;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 10px;
                font-weight: bold;
                transform: rotate(-15deg);
                margin: 0 auto;
            }
            
            .no-print {
                display: none;
            }
            
            .watermark {
                position: fixed;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%) rotate(-45deg);
                font-size: 60px;
                color: rgba(0,0,0,0.1);
                font-weight: bold;
                z-index: -1;
                opacity: 0.3;
            }
        }
        
        @media screen {
            body {
                background: #f4f4f4;
                padding: 20px;
                font-family: Arial, sans-serif;
            }
            
            .print-container {
                background: white;
                padding: 40px;
                max-width: 210mm;
                margin: 0 auto;
                box-shadow: 0 0 20px rgba(0,0,0,0.1);
            }
            
            .print-actions {
                text-align: center;
                margin: 20px 0;
                padding: 20px;
                background: #f8f9fa;
                border-radius: 8px;
            }
            
            .print-btn {
                padding: 12px 25px;
                background: #007bff;
                color: white;
                border: none;
                border-radius: 5px;
                cursor: pointer;
                margin: 0 10px;
                text-decoration: none;
                display: inline-block;
            }
            
            .applicant-container {
                display: flex;
                gap: 30px;
                margin-bottom: 30px;
                align-items: flex-start;
            }
            
            .photo-section {
                flex: 0 0 150px;
                text-align: center;
                border: 2px solid #000;
                padding: 15px;
                background: #fff;
            }
            
            .candidate-photo {
                width: 120px;
                height: 150px;
                object-fit: cover;
                border: 1px solid #ccc;
            }
            
            .applicant-info {
                flex: 1;
                border: 2px solid #000;
                padding: 20px;
                background: #f9f9f9;
            }
        }
    </style>
</head>
<body>
    <div class="print-container">
        <div class="print-actions no-print">
            <button onclick="window.print()" class="print-btn">🖨️ Print Application</button>
            <a href="/application/{{application.serial_no}}" class="print-btn">← Back to Details</a>
        </div>
        
        <!-- Watermark -->
        <div class="watermark">OFFICIAL COPY</div>
        
        <!-- Header -->
        <div class="print-header">
            <h1>OFFICIAL APPLICATION FORM</h1>
            <div class="serial-no">SERIAL NUMBER: {{application.serial_no}}</div>
            <div>APPLICATION DATE: {{application.created_at}}</div>
        </div>
        
        <!-- Applicant Info with Photo -->
        <div class="applicant-container">
            <!-- Photo Section -->
            <div class="photo-section">
                {{#if candidate_photo}}
                <img src="/uploads/{{application.serial_no}}/{{candidate_photo}}" 
                     alt="Candidate Photo" class="candidate-photo">
                {{else}}
                <div style="width: 100px; height: 120px; background: #eee; display: flex; align-items: center; justify-content: center; margin: 0 auto;">
                    No Photo
                </div>
                {{/if}}
                <div class="photo-label">PASSPORT PHOTO</div>
                <div class="photo-label">35×45 mm</div>
            </div>
            
            <!-- Personal Information -->
            <div class="applicant-info">
                <h3 style="margin: 0 0 15px 0; border-bottom: 1px solid #000; padding-bottom: 5px;">PERSONAL INFORMATION</h3>
                <div class="info-grid">
                    <div class="info-item"><strong>Name:</strong> {{application.applicant_name}}</div>
                    <div class="info-item"><strong>Father's Name:</strong> {{application.father_name}}</div>
                    <div class="info-item"><strong>CNIC:</strong> {{application.cnic}}</div>
                    <div class="info-item"><strong>Religion:</strong> {{application.religion}}</div>
                    <div class="info-item"><strong>Caste:</strong> {{application.caste}}</div>
                    <div class="info-item"><strong>Domicile:</strong> {{application.domicile}}</div>
                    <div class="info-item"><strong>Date of Birth:</strong> {{application.dob}}</div>
                    <div class="info-item"><strong>CNIC Expiry:</strong> {{application.cnic_expiry}}</div>
                    <div class="info-item"><strong>Form No:</strong> {{application.application_form}}</div>
                    <div class="info-item"><strong>Status:</strong> <span style="font-weight: bold; color: #28a745;">{{application.status}}</span></div>
                </div>
            </div>
        </div>
        
        <!-- Documents Section -->
        <div class="documents-section">
            <h3>ATTACHED DOCUMENTS</h3>
            <div class="documents-list">
                {{#if photos.length}}
                <div class="document-item"><strong>CNIC Photos:</strong></div>
                <div class="document-item">
                    {{#each photos}}
                    • {{this}}<br>
                    {{/each}}
                </div>
                {{/if}}
                
                {{#if documents.length}}
                <div class="document-item"><strong>Supporting Documents:</strong></div>
                <div class="document-item">
                    {{#each documents}}
                    • {{this}}<br>
                    {{/each}}
                </div>
                {{/if}}
                
                {{#if candidate_photo}}
                <div class="document-item"><strong>Candidate Photo:</strong></div>
                <div class="document-item">• {{candidate_photo}}</div>
                {{/if}}
            </div>
        </div>
        
        <!-- Signatures -->
        <div style="display: flex; justify-content: space-between; margin-top: 50px;">
            <div class="signature-area" style="flex: 1;">
                <div class="signature-line"></div>
                <div class="signature-label">APPLICANT'S SIGNATURE</div>
                <div style="font-size: 10px; margin-top: 5px;">Date: ________________</div>
            </div>
            
            <div class="signature-area" style="flex: 1;">
                <div class="signature-line"></div>
                <div class="signature-label">AUTHORIZED SIGNATORY</div>
                <div style="font-size: 10px; margin-top: 5px;">Date: ________________</div>
            </div>
        </div>
        
        <!-- Stamp Area -->
        <div class="stamp-area">
            <div class="stamp">
                OFFICIAL<br>STAMP
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <p>This is an official computer-generated document. No physical signature is required.</p>
            <p>Generated on: {{currentDate}}</p>
            <p>Application ID: {{application.serial_no}} | Reference: {{application.id}}</p>
        </div>
    </div>
    
    <script>
        // Format dates for display
        document.addEventListener('DOMContentLoaded', function() {
            // Update current date
            const currentDateEl = document.querySelector('.footer p:nth-child(2)');
            if (currentDateEl) {
                currentDateEl.textContent = 'Generated on: ' + new Date().toLocaleString();
            }
        });
    </script>
</body>
</html>